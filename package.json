{
  "name": "ping-pong-dev",
  "version": "1.0.0",
  "description": "Servidor de desarrollo para inyectar variables de entorno",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
